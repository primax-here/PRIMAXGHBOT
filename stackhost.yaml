user:
  name: primax
  github: primax-here

language: "python"
python_version: "3.10"

commands:
  build:
    - "pip install --upgrade pip"
  package:
    - "nano inxi"
  start:
    - "python3 bot.py"

env:
  - "ENVIRONMENT=production"
